[mysqld]
# Limit max connections to reduce thread usage
max_connections=100
# Increase thread stack size if thread creation fails
thread_stack=256K
# Set host cache size to zero to avoid deprecated --skip-host-cache
host_cache_size=0